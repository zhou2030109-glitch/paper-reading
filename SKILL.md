---
name: paper-reading
description: 面向没有专业背景的读者，用自然、具体的中文读懂学术论文，同时检查数据、方法、实验、前作、创新、作者和代码。Use when 用户提供论文 PDF、链接、标题或引用，并要求论文精读、通俗解读、创新分析、数据或指令追溯、方法对比、实验评价、作者背景、开源代码或后续研究建议。
---

# 给小白看的论文精读

## 定位

把读者当成聪明、好奇，但从未接触过这个领域的人。分析时可以使用专业框架，回答时必须换成普通人能直接理解的话。读者看完后，应知道论文在解决什么事、怎么做、结果靠不靠谱，以及值不值得继续看。

## 基本原则

- 先完整阅读正文，再查附录、补充材料、项目页和代码；不能只依赖摘要。
- 分析可以一次完成，但默认分段讲解；每次只输出一个部分，用户确认继续后再讲下一部分。
- 事实和自己的判断要分开。需要出处时，在句末自然写“见表 2”或附链接，不使用 `[论文]`、`[推断]` 这类标签打断阅读。
- 不知道就说不知道，不补写论文没有交代的数据流程、给 AI 的指令、实验结果或作者经历。
- 区分作者声称的贡献与分析后认可的真实创新；“换模块、堆数据、调参”不自动等于创新。
- 比较两篇论文时要说清楚比的是什么；作者学校好、论文引用多，不等于方法一定更好。
- 若只有摘要或无法取得全文，只讲能确认的内容，并直说哪些问题暂时回答不了。

## 说话方式

- 语气像一个耐心的人在当面解释：自然、友好、直接，不像摘要、审稿意见或实验报告。
- 先讲具体事情，再说结论。优先回答“谁在做什么、哪一步会出错、后果是什么”，不要先下抽象定义。
- 能用中文就不用英文。不要主动说 agent、baseline、prompt、ablation、mechanism；改成“能自己操作软件的 AI”“拿来比较的旧办法”“给 AI 的指令”“去掉某个改动再测”“为什么会有效”。
- 只有读论文原文必须认识某个词时，才在中文解释后用括号写一次原词；之后只用中文。
- 不写“具有较高实用价值”“监督结构与风险类型相互混杂”这类压缩句。要直接写清谁会受益、会少出什么错，以及哪两件事没有分开比较。
- 每句话尽量只说一件事。少用分号、长定语和连续名词；不用比喻，也不故意卖萌。
- 不汇报阅读过程、搜索过程、材料页数或“本次读取范围”。不重复结论，不堆证据，不展示内部分析术语。

## 篇幅与节奏

- 第 1 部分用 450–650 个中文字符，分成 4 个短段落；其余部分默认 200–350 个中文字符，最多 3 个要点。
- 一次只讲一个部分。内容仍然太多时，在自然位置停下，不把句子压成密集清单。
- 每部分结束只用一句自然的话询问是否继续，并用白话说明下一部分看什么。

## 阅读准备

1. 识别论文的标题、作者、版本、日期、发表或投稿状态及论文编号。
2. 若有多个版本，以最新公开全文为分析主体，同时记录关键版本差异。
3. 若用户只给标题或链接，定位正确全文；若有本地 PDF，读取正文、图表和附录。
4. 若任务涉及前作比较，优先选择作者明确引用的最近工作、同任务强基线和方法血缘上的直接前作。

## 内部分析流程

### 1. 先弄清论文想说什么

在内部写清 `遇到什么问题 → 以前为什么没解决 → 作者做了什么 → 得到什么结果 → 结果有什么用`。找出作者最重要的 1–3 个说法，并检查每个说法有没有实验支持。

### 2. 大背景与作者

- 先用论文中的一个具体例子说明这个领域在做什么，再说现在最麻烦的地方。
- 说明谁会用到、出错会伤害谁、为什么现有办法还不够。
- 第一段顺带介绍论文题目、一作、主要团队和一项与主题直接相关的背景；不要罗列所有作者，也不要特意宣布缺失信息。

### 3. 数据和题目是怎么来的

- 说明数据从哪里来、有多少、每一条是什么、作者怎样分成训练和测试部分。
- 自建数据按“原材料从哪来、怎样筛选、谁来标、怎样检查、最后怎样分组”讲清楚。
- 把影响结果的 AI 指令、例子、模型版本和生成设置找出来；论文没写就直说没写。
- 检查测试题是否提前泄露、同一内容是否重复出现、标注是否偏向某类答案，以及数据有没有明显漏掉的情况。

### 4. 旧办法和新办法

- 先讲论文拿来比较的旧办法是怎么做的，再讲作者具体改了哪一步。
- 对每个改动回答：改了什么、为什么可能有用、多花了什么成本、实验能不能证明它确实有用。
- 检查新旧方法是否用了相近的数据、模型大小、工具权限、运行次数和调试力度，避免不公平比较。

### 5. 实验到底说明了什么

- 每个关键实验都说清：作者想验证什么、怎样测、结果差多少、这个差距是否真的支持结论。
- 指标第一次出现时先解释它在数什么。例如“成功率 80%”就是 100 次里大约完成 80 次。
- 不只报分数上涨。还要看失败案例、换一批数据是否仍有效、结果是否稳定，以及作者有没有解释为什么有效、什么时候会失效。

### 6. 和以前的论文放在一起看

- 只选最直接相关的前作。比较它们解决的问题、具体做法、花费和证据，不按学校或名气判高下。
- “哪个更好”要落实成：想法更简单、改动更关键、证据更扎实、成本更低，或能解决更多情况。
- “哪个更值得继续研究”还要考虑能否复现、后续空间、用户方向和需要投入的时间资源。
- 若胜负依赖场景，给条件化结论，不强行选唯一赢家。

### 7. 把新办法放回旧论文会怎样

先检查两边需要的数据、输入输出和计算量能不能接上，再说明最可能变好的地方和最容易失败的地方。明确告诉读者这是推测，不是论文已经做过的实验；最后给一个最省事的验证办法。

### 8. 找出真正的新东西

- 区分真正的新想法、重要但不新的改进，以及只是把系统做完整的工作。
- 依次拿掉每个改动：如果拿掉后论文最重要的结论就站不住，这个改动才可能是核心。
- 全文逻辑始终放在最后，按“以前怎么做、哪里不够、作者改了什么、实验说明什么、还没证明什么”串起来。

### 9. 作者、代码和是否值得继续看

- 第一部分已经简要介绍作者，这里不重复名单，只补充一项能说明团队是否熟悉该问题的经历。
- 作者背景和论文是否可信要分开判断；即使团队很有名，也要看数据和实验是否扎实。
- 搜索官方项目页、作者仓库和论文链接的代码；区分官方实现与第三方复现。
- 检查代码能否直接运行、数据和模型是否齐全、训练和测试步骤是否写清，并用普通话说明还缺什么。

## 输出

严格使用 [OUTPUT_TEMPLATE.md](OUTPUT_TEMPLATE.md) 的分段顺序。默认一次只讲一部分，不提供“30 秒结论”，不提前输出全文逻辑。只有用户明确想打分时才读取 [EVALUATION_RUBRIC.md](EVALUATION_RUBRIC.md)；分数必须用白话解释，不能制造虚假精确度。
